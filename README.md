# SubMonitor Pages shell

Public static shell for `https://fluids.ac/submissions/`.

This repository intentionally contains no paper data, application password, session secret, or GitHub data token. Authentication and private data access are handled by the separate API at `https://api.fluids.ac`.

The source application and private data live in the private `hweifluids/SubMonitor_Huanxia` repository.
